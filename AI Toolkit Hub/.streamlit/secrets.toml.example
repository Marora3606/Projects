# Copy this file to secrets.toml and fill in your own values.
# secrets.toml is gitignored; this .example template is safe to commit.
#
# Streamlit reads this automatically — access values with st.secrets["KEY"].
# Docs: https://docs.streamlit.io/develop/concepts/connections/secrets-management

OPENAI_API_KEY = ""
OPENAI_BASE_URL = "https://api.bluesminds.com/v1"

# Signing key for the JWTs issued in Home.py. Generate one with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY = ""
