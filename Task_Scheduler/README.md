# Task Scheduler

A modular task management application built in Python.

## Structure

- `main.py` starts the program
- `task.py` defines the `Task` class
- `scheduler.py` manages task storage and operations
- `utils.py` contains helper functions
- `menu.py` contains all interactive menu functions

## Run

```bash
python3 main.py
```
